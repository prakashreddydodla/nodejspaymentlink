apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejspaymentlink.fullname" . }}
  labels:
    {{- include "nodejspaymentlink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejspaymentlink.selectorLabels" . | nindent 4 }}
